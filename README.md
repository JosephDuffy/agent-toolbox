# agent-toolbox

The is repo contains a set of files intended for use with LLMs.

The information here is _heavily_ opinionated and is not intended to be applied to all projects; this is not a "make your LLM write code correctly" toolbox but rather a "make your LLM write code that follows my personal convensions."
